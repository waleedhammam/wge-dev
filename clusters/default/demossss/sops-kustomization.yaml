apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  annotations:
    sops-public-key/name: sops-gpg-pub
    sops-public-key/namespace: flux-system
  creationTimestamp: null
  name: my-secrets
  namespace: flux-system
spec:
  decryption:
    provider: sops
    secretRef:
      name: sops-gpg
  interval: 10m0s
  path: clusters/default/demossss/sops
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
status: {}
